import { useT } from "next-i18next/client";

export const useTSettings = () => useT("common/settings");

export const useTUtils = () => useT("common/utils");

export const useTFormErrors = () => useT("common/errors/forms");
